# Copy to ops/secrets.env, fill in, then: chmod 600 ops/secrets.env
# Nothing here is ever committed - ops/secrets.env is gitignored.
export WHOOP_CLIENT_ID=
export WHOOP_CLIENT_SECRET=
export INTERVALS_ICU_API_KEY=
# Only needed if the Sunday summary sends itself through the Gmail relay.
export SMTP_PASSWORD=
